.chat-avatar {
    background-color: var(--emerald);
    border-radius: 100px;
    border: 1px solid var(--black);
    width: 50px;
    height: 50px;
}

.profile-avatar {
    background-color: var(--grey);
    width: 362px;
    height: 362px;
}