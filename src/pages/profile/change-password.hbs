<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Profile</title>
    <link rel="stylesheet" href="../../index.pcss"/>
</head>
<body>
    <main class="container">
        <form class="profile" action="#" method="post">
            <div class="profile-edit__password-fieldset">
               <label for="password" class="profile-edit__password-label">Password</label>
               {{> field/field name="password" type="password" placeholder="Password" class="edit-password"}}
               <label for="oldPassword" class="profile-edit__password-label">Old password</label>
               {{> field/field name="oldPassword" type="password" placeholder="Password" class="edit-password"}}
               <label for="newPassword" class="profile-edit__password-label">New password</label>
               {{> field/field name="newPassword" type="password" placeholder="Password" class="edit-password"}}
            </div>
            <div class="profile__buttons">
                 {{> button/buttonSubmit label="Save changes" class="profile-button__edit"}}
            </div>
        </form>
    </main>
</body>
